# initialize
on: initialize
	#
	$a = "xxx"
	console.printLine($xxx)
	#
	# console.printLine("getSize() of $a via $a->getSize(): " + $a->getSize())
	console.printLine("getSize() of 'abc' via 'abc'->getSize(): " + 'abc'->getSize())
end

# if no condition is met, nothing will be executed, lol :D
on: nothing
	script.stop()
end

# an error has occurred
on: error
	script.stop()
end